/**
 * Home Screen - Fallback (re-exports native implementation)
 * 
 * Platform-specific implementations:
 * - index.native.tsx - Native with drawer navigation
 * - index.web.tsx - Web with persistent sidebar
 */
export { default } from './index.native';
