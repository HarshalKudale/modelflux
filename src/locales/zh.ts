// Chinese (Simplified) language bundle (placeholder)
// TODO: Add proper translations

import { en } from './en';

// Start with English as base, override with Chinese translations as they become available
export const zh: Record<string, string> = {
    ...en,
    // Override with Chinese translations here
};
