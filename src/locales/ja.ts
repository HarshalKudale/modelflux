// Japanese language bundle (placeholder)
// TODO: Add proper translations

import { en } from './en';

// Start with English as base, override with Japanese translations as they become available
export const ja: Record<string, string> = {
    ...en,
    // Override with Japanese translations here
};
