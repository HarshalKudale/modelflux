// French language bundle (placeholder)
// TODO: Add proper translations

import { en } from './en';

// Start with English as base, override with French translations as they become available
export const fr: Record<string, string> = {
    ...en,
    // Override with French translations here
};
