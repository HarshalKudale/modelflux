/**
 * RAG Core Module Exports
 * 
 * Note: embeddingFactory and sourceProcessor are platform-specific
 * and should be imported directly from their paths (Metro will resolve
 * the correct .native.ts or .web.ts file based on platform).
 */

export * from './types';
